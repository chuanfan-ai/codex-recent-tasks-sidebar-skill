# Agent Instructions

完整阅读并遵循 `skills/codex-recent-tasks-sidebar/SKILL.md`，从仓库模板构建“本机AI状态栏”。

优先复用仓库内模板和脚本，不要从零重写。交付前运行 `./scripts/qa.sh`，再在真实 macOS 桌面环境检查三类 Agent、240 像素窄栏、全局置顶和底边吸附。不得读取、打印、复制、提交或上传真实 Codex、QwenWorkCN、Kimi 任务内容、ID、数据库、会话文件或凭证。覆盖 `/Applications` 前必须获得用户授权。
